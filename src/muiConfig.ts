import { createTheme } from "@mui/material"

const theme = createTheme({
    palette: {
        primary: {
            light: '#1976d2',
            main: '#1565c0',
            dark: '#01579b'
        }
    }
})
export default theme